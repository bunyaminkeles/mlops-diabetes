name: Model Egitim Robotu (CT)
on:
  # 1. Manuel Buton (Yine kalsın)
  workflow_dispatch:

  # 2. ESKİ YÖNTEM: Push (Bunu tutabilirsin, sen elle yükleyince çalışır)
  push:
    paths:
      - 'train.py'
      - 'data.csv'

  # 3. YENİ YÖNTEM: Diğer Robot İşini Bitirince Çalış (Zincirleme)
  workflow_run:
    workflows: ["Gunluk Veri Ureticisi (Cron Job)"] # Buraya daily_schedule.yml içindeki "name:" neyse onu tam yazmalısın!
    types:
      - completed

jobs:
  train-and-save:
    runs-on: ubuntu-latest
    
    # Robota yazma izni veriyoruz
    permissions:
      contents: write

    steps:
    # 1. Kodları Çek
    - name: Kodlari Getir
      uses: actions/checkout@v3

    # 2. Python Kur
    - name: Python Kurulumu
      uses: actions/setup-python@v4
      with:
        python-version: "3.9"

    # 3. Kütüphaneleri Yükle
    - name: Gereklilikleri Yukle
      run: |
        pip install --upgrade pip
        pip install -r requirements.txt

    # 4. MODELİ EĞİT (Burası Show Kısmı)
    - name: Modeli Egit
      run: python train.py
      # Bu komut çalıştığında 'diabetes_model.pkl' dosyası yenilenir.

    # 5. Yeni Modeli Kaydet ve GitHub'a Gönder
    - name: Yeni Modeli Commit ve Push Yap
      run: |
        git config --global user.name "GitHub Action Robotu"
        git config --global user.email "robot@github.com"
        
        # Değişiklik var mı kontrol et
        git add diabetes_model.pkl
        
        # Eğer model değişmediyse hata verme, devam et
        git commit -m "🤖 Robot: Yeni model eğitildi ve güncellendi" || echo "Model değişmedi, commit atılmadı."
        
        git push

    # 6. Render'ı Tetikle (Zile Basma İşlemi)
    - name: Render Deploy Tetikle
      # Sadece commit atıldıysa çalışsın (Hata varsa boşuna deploy etmesin)
      if: success() 
      run: curl "${{ secrets.RENDER_DEPLOY_HOOK }}"  